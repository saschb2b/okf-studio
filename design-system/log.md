## 2026-07-08
* **Creation**: A [viz card](components/viz-card.md) component: a screenshot with its title and one caption line on a single bordered surface, the media bleeding flush to the card edge behind a hairline seam (the [hero](patterns/hero.md)'s grounded-media motif at card scale). Built for the landing page's new visualization section, a two-column grid of the app's four views of one bundle (graph, treemap, sunburst, circle packing). Reserved `aspect-ratio` keeps the lazy-loaded grid from shifting. Runnable example added; indexed; `styles/components.css` updated.

## 2026-07-06
* **Update**: Two [hero](patterns/hero.md) composition refinements from owner feedback. (1) **Breathing room below the resting pill**: the canvas top margin grew `space.12` to `space.24` (`space.16` on narrow screens) so the nav reads as a floating object rather than a cap glued to the canvas edge. (2) The product shot shows through a **windowed crop** (about the top 60% of the capture) instead of full height, and a CSS scroll-driven pan (`animation-timeline: view()`) walks the window down the capture as the page scrolls, the Apple-style reveal; browsers without support keep the static crop, reduced motion opts out, and the spotlight zoom still shows the full capture. Verified at load and mid-scroll at 1440px.
* **Update**: [Spacing](foundations/spacing.md) gains `size.canvas` (1680px), capping full-bleed contained surfaces on ultrawide displays: an unbounded hero canvas read as empty acreage around `size.container` content and drifted away from the nav pill's width family. Projected to `--size-canvas` in styles/tokens.css.
* **Creation**: A [showcase panel](patterns/showcase.md) pattern: an alternating feature row as a contained surface (the canvas family) with the product screenshot bleeding flush to the panel's edge behind a hairline seam, copy vertically centered on the other half. Replaces the bare bordered screenshot floating beside copy on the page background, the last of the landing page's floating slabs. Runnable example added; indexed; the nav's index line updated to "floating pill bar".
* **Update**: The [site nav](components/nav.md) becomes a **floating pill**: fully rounded, hairline-bordered on all sides, translucent surface with a backdrop blur and a small shadow, actions inside taking its curvature. It replaces the page-wide bottom-bordered strip, which the canvas restyle had first solved by tucking the nav inside the canvas non-sticky; owner feedback wanted the Download action reachable while scrolling, and sticky cannot live inside the canvas (its overflow clipping breaks sticky positioning), so the pill floats just above the canvas at rest and hovers over content while scrolling. Component concept, both examples, `styles/components.css`, and the [hero](patterns/hero.md) composition updated together; verified in the built site at rest, mid-scroll, and 375px.
* **Update**: The [hero](patterns/hero.md) gains a canvas and the [download grid](patterns/download.md) a matching band, restyling the landing page from three disconnected strips into intentional contained surfaces. The canvas is a rounded (radius.xl), hairline-bordered container inset from the viewport that bundles the nav, headline, CTAs, and product shot into one object (Gestalt common region); brand tints (color-mix of secondary/primary into transparency) and the node-dot grid render inside it, clipped to the radius, instead of floating over the page. The product shot is grounded flush against the canvas foot (top corners only, border-strong, no glow); the nav lives inside the canvas transparent and unbordered, collapsing to brand + primary action on narrow screens. The download band mirrors the canvas so the page opens and closes on the same shape. Both pattern concepts and runnable examples updated together; verified in the built site at 1440px and 375px.

## 2026-07-02
* **Update**: [Download button](components/download-button.md) gains a trailing `.dl-btn__arrow` (`↓`) pushed to the card's right edge, dim at rest and primary + nudged 2px down on hover (instant under reduced motion). The cards read as info tiles without it; the arrow makes them read as actions. Anatomy, states, both runnable examples, and `styles/components.css` updated together.

## 2026-07-01
* **Update**: Removed em dashes from concept descriptions, body prose, and stylesheet comments for a plainer copy voice.
* **Creation**: Authored the OKF Viewer Design System: color, typography, spacing, shape, elevation, and motion foundations; button, download-button, nav, and feature-card components; hero and download-grid patterns; the dark-first guideline; and the token/component CSS. Derived from the app theme and app icon in a zed.dev-inspired dark-first register.
