## 2026-07-26
* **Fix**: Resynced the brand roles with the desktop app's dark theme, which had moved out from under this bundle. `primary` `#5B8CFF` to `#74A0FF`, `primary-hover` to `#92B4FF`, `focus` with it, and `error` `#FF6B5E` to `#FF7F72`; `warning` and `success` were already in step. The app's accent moved so a colored label on a hovered row inside a dialog could clear 4.5:1 — a constraint this page does not have — but the accent is the product's one identity color, and a visitor should not meet two different blues. Adopting it only adds headroom here: `primary` on `bg` goes 6.22:1 to 7.69:1.
* **Update**: Split the relationship to the app theme into two explicit tables in [color](foundations/color.md) — the roles that track it, and the surfaces and text roles that deliberately do not, with the reason. "Derived from the app theme" was true when it was written and silently stopped being true.
* **Fix**: Corrected the contrast table, whose numbers were not derivable from the tokens beside them: it claimed 17.4, 8.4, 5.2, and 6.2 where the values were 16.96, 7.46, 6.22, and 6.22. Close enough to pass review, which is the problem. `scripts/check-design-system.mjs` now re-derives every pairing, checks the tracked roles against `src/styles.css`, and verifies `styles/tokens.css` is a faithful projection of the foundation frontmatter — the header called that projection mechanical, and nothing enforced it.

## 2026-07-25
* **Update**: [Typography](foundations/typography.md) moves off the system stack. `font.sans` is Inter Variable and `font.mono` is JetBrains Mono Variable, both self-hosted by the consuming site so there is no third-party font request and no layout that changes shape with the reader's operating system; the system stacks remain as the fallback. The scale came down with it: display from a 72px ceiling to 52px, `text.h1` from 44px to 34px, `text.h2` from 28px to 24px, `text.h3` from 20px to 18px, and `text.sm` up from 14px to 15px. A 72px headline spanned the whole container, wrapped unpredictably, and read as a poster; hierarchy now comes from weight, color, and space. Adds `font.features-sans`, `leading.relaxed`, and `tracking.tighter`, and pairs with the new `size.headline` cap.
* **Update**: [Color](foundations/color.md) restates the accent rule: `colors.primary` marks what a reader can act on (links, the one primary button, focus) and nothing else. `colors.gradient-brand` is demoted to artwork, where it came from; the icon keeps it, the UI does not. `colors.secondary` survives only as a background tint at 8 percent or less, which is where the hero canvas and download band tints landed (down from 16 and 12 percent).
* **Update**: Two [color](foundations/color.md) values changed for contrast. `colors.text-dim` moves from `#6C717B` to `#7B808A`: it came to 4.0:1 on `bg` and less on `surface`, and it carries the footer, the download notes, every mono meta line, and every eyebrow, all of which are normal-size text at 13px. `colors.on-primary` moves from white to `#0B0B0D`: white on `colors.primary` was 3.2:1, so the primary button's own label failed the threshold. Darkening the accent instead would have broken it as a link color on a near-black page, so the bright fill takes dark text. The concept now carries the full pairing table.
* **Update**: [Elevation](foundations/elevation.md) drops `glow.brand` and `glow.brand-strong`. Nothing consumed the strong one, and the soft one sat under the primary button on hover, which is the clearest tell of a template landing page. Depth on dark comes from the `bg`/`surface`/`surface-2` steps and a hairline.
* **Update**: [Button](components/button.md) primary becomes a solid `colors.primary` fill hovering to `colors.primary-hover`, replacing the gradient face, the glow, and the 1px lift: three signals for one control. Adds a `--lg` size for the single headline action on a page, and drops the default to `text.sm` so the same component fits the nav pill.
* **Update**: [Feature card](components/feature-card.md) and [viz card](components/viz-card.md) no longer rise on hover; the border is the whole feedback. Feature card gains the rule that a card grid must fill its rows, after the home page's five trust claims rendered as three across with two orphans.
* **Update**: The [site nav](components/nav.md) pill goes from 84 to 94 percent opacity. Headings scrolling beneath it stayed legible through the surface, so a section title read as sliced in half as it passed behind the bar.
* **Update**: The [hero](patterns/hero.md) headline is solid `colors.text` capped at `size.headline`, not a `background-clip: text` gradient fill: lower contrast on the words carrying the claim, gone entirely in forced-colors mode, and the most-copied device on the software landing pages of the last few years. The accent moves to the button beneath it.
* **Rewrite**: [Showcase](patterns/showcase.md) becomes a section rather than a panel: a two-column copy header over one full-container 16:9 figure. The alternating half-and-half panel gave a 1760px capture a 550px column, a 30 percent scale at which every label in the app rendered at three or four pixels; the composition looked considered and proved nothing. Full width doubles the scale and a shared aspect ratio equalizes the media across stories.
* **Creation**: A [definition rows](components/deflist.md) component: parallel named claims as hairline-divided rows on one surface, term left and description right. A responsive card grid places items into whole columns, so a set whose count is not a multiple of the column count ends with a hole; the home page's five boundaries and three of the four capability pages rendered exactly that way. Rows take any count, give every term the same starting x-position, and cut a page of eighteen cards to roughly half its height.
* **Creation**: A [code sample](components/code.md) component: a named file and its literal contents on one surface, in two levels of emphasis (structure, contents, annotation) rather than a syntax rainbow, which would break the accent rule and compete with the screenshots. Built for the home page's format section, where eleven lines of real frontmatter do the work a paragraph about frontmatter was failing to do.
* **Update**: [Spacing](foundations/spacing.md) adds `size.headline` (20ch), narrows `size.measure` from 42rem to 40rem, and moves the section rhythm from `space.96`/`space.128` to `space.80`. Generous everywhere meant nothing was grouped and the page read as unrelated slabs with holes between them.

## 2026-07-19
* **Update**: The [site nav](components/nav.md) now supports the multi-page product site. Product routes sit in a click and keyboard disclosure, narrow screens retain every destination in a mobile menu, and the active route stays visible. Escape and outside interaction close open panels, and focus returns to the trigger. The runnable examples and `styles/components.css` cover both layouts.
* **Update**: [Feature card](components/feature-card.md) now treats its glyph and detail link as optional. The body remains a concise value statement, and a card may link to one detail route without adding another button.

## 2026-07-16
* **Creation**: A [function-first](guidelines/function-first.md) guideline capturing the Zed power-tool stance: docked surfaces (composer, shelves, status strips) run edge-to-edge behind a single hairline; radius and enclosing borders are reserved for floating surfaces (popovers, dialogs) and blocks inside a scrolling document; keyboard focus on an edge-to-edge editor tints the hairline instead of drawing a ring. The desktop app's agent composer is the worked case and was reworked to match. Overview gains the matching principle; indexed.

## 2026-07-12
* **Update**: Updated the hero and feature-card examples to the shipped Studio proposition: connected knowledge, an explicit ACP agent connection, and no implied bundle write. Component structure, tokens, and behavior remain unchanged.

## 2026-07-11
* **Update**: Renamed the product labels in the design-system overview and runnable component/pattern examples from **OKF Viewer** to **OKF Studio**. Tokens, component behavior, and layout remain unchanged.

## 2026-07-08
* **Creation**: A [viz card](components/viz-card.md) component: a screenshot with its title and one caption line on a single bordered surface, the media bleeding flush to the card edge behind a hairline seam (the [hero](patterns/hero.md)'s grounded-media motif at card scale). Built for the landing page's new visualization section, a two-column grid of the app's four views of one bundle (graph, treemap, sunburst, circle packing). Reserved `aspect-ratio` keeps the lazy-loaded grid from shifting. Runnable example added; indexed; `styles/components.css` updated.

## 2026-07-06
* **Update**: Two [hero](patterns/hero.md) composition refinements from owner feedback. (1) **Breathing room below the resting pill**: the canvas top margin grew `space.12` to `space.24` (`space.16` on narrow screens) so the nav reads as a floating object rather than a cap glued to the canvas edge. (2) The product shot shows through a **windowed crop** (about the top 60% of the capture) instead of full height, and a CSS scroll-driven pan (`animation-timeline: view()`) walks the window down the capture as the page scrolls, the Apple-style reveal; browsers without support keep the static crop, reduced motion opts out, and the spotlight zoom still shows the full capture. Verified at load and mid-scroll at 1440px.
* **Update**: [Spacing](foundations/spacing.md) gains `size.canvas` (1680px), capping full-bleed contained surfaces on ultrawide displays: an unbounded hero canvas read as empty acreage around `size.container` content and drifted away from the nav pill's width family. Projected to `--size-canvas` in styles/tokens.css.
* **Creation**: A [showcase panel](patterns/showcase.md) pattern: an alternating feature row as a contained surface (the canvas family) with the product screenshot bleeding flush to the panel's edge behind a hairline seam, copy vertically centered on the other half. Replaces the bare bordered screenshot floating beside copy on the page background, the last of the landing page's floating slabs. Runnable example added; indexed; the nav's index line updated to "floating pill bar".
* **Update**: The [site nav](components/nav.md) becomes a **floating pill**: fully rounded, hairline-bordered on all sides, translucent surface with a backdrop blur and a small shadow, actions inside taking its curvature. It replaces the page-wide bottom-bordered strip, which the canvas restyle had first solved by tucking the nav inside the canvas non-sticky; owner feedback wanted the Download action reachable while scrolling, and sticky cannot live inside the canvas (its overflow clipping breaks sticky positioning), so the pill floats just above the canvas at rest and hovers over content while scrolling. Component concept, both examples, `styles/components.css`, and the [hero](patterns/hero.md) composition updated together; verified in the built site at rest, mid-scroll, and 375px.
* **Update**: The [hero](patterns/hero.md) gains a canvas and the [download grid](patterns/download.md) a matching band, restyling the landing page from three disconnected strips into intentional contained surfaces. The canvas is a rounded (radius.xl), hairline-bordered container inset from the viewport that bundles the nav, headline, CTAs, and product shot into one object (Gestalt common region); brand tints (color-mix of secondary/primary into transparency) and the node-dot grid render inside it, clipped to the radius, instead of floating over the page. The product shot is grounded flush against the canvas foot (top corners only, border-strong, no glow); the nav lives inside the canvas transparent and unbordered, collapsing to brand + primary action on narrow screens. The download band mirrors the canvas so the page opens and closes on the same shape. Both pattern concepts and runnable examples updated together; verified in the built site at 1440px and 375px.

## 2026-07-02
* **Update**: [Download button](components/download-button.md) gains a trailing `.dl-btn__arrow` (`↓`) pushed to the card's right edge, dim at rest and primary + nudged 2px down on hover (instant under reduced motion). The cards read as info tiles without it; the arrow makes them read as actions. Anatomy, states, both runnable examples, and `styles/components.css` updated together.

## 2026-07-01
* **Update**: Removed em dashes from concept descriptions, body prose, and stylesheet comments for a plainer copy voice.
* **Creation**: Authored the OKF Viewer Design System: color, typography, spacing, shape, elevation, and motion foundations; button, download-button, nav, and feature-card components; hero and download-grid patterns; the dark-first guideline; and the token/component CSS. Derived from the app theme and app icon in a zed.dev-inspired dark-first register.
