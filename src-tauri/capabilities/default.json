{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "OKF Viewer least-privilege capabilities: dialog (folder picker), store (settings/recent), opener (resource URLs in the OS browser). No network, no webview filesystem access — the Rust core does all reads.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging",
    "core:window:allow-start-resize-dragging",
    "core:window:allow-minimize",
    "core:window:allow-toggle-maximize",
    "core:window:allow-close",
    "core:window:allow-is-maximized",
    "dialog:default",
    "store:default",
    "opener:default"
  ]
}
