{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "OKF Studio webview capabilities: dialog, store, opener, an opt-in updater with restart, window controls, and pop-out creation. No webview filesystem or general network API is granted; Rust owns explicit bundle, provider, source, installer, and agent operations. 'pop-*' covers reader windows with the same surface and no extra plugin grants.",
  "windows": ["main", "pop-*"],
  "permissions": [
    "core:default",
    "core:webview:allow-create-webview-window",
    "core:window:allow-start-dragging",
    "core:window:allow-start-resize-dragging",
    "core:window:allow-minimize",
    "core:window:allow-toggle-maximize",
    "core:window:allow-close",
    "core:window:allow-is-maximized",
    "dialog:default",
    "store:default",
    "opener:default",
    "updater:default",
    "process:allow-restart"
  ]
}
