{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "OKF Studio webview capabilities: store, opener, an opt-in updater with restart, window controls, and pop-out creation. No webview dialog, filesystem, or general network API is granted; Rust owns explicit bundle, provider, source, installer, and agent operations. 'pop-*' covers reader windows with the same surface and no extra plugin grants.",
  "windows": ["main", "pop-*"],
  "permissions": [
    "core:default",
    "core:webview:allow-create-webview-window",
    "core:window:allow-start-dragging",
    "core:window:allow-start-resize-dragging",
    "core:window:allow-minimize",
    "core:window:allow-toggle-maximize",
    "core:window:allow-close",
    "core:window:allow-is-maximized",
    "store:default",
    "opener:default",
    "updater:default",
    "process:allow-restart"
  ]
}
